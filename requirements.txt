nltk>=3.9
torch>=2.6
transformers>=4.44
datasets>=3.0
numpy>=1.26
pillow>=10.0

rich>=13.7
pygame-ce>=2.5
discord.py>=2.4
fastapi>=0.115
uvicorn>=0.30
python-dotenv>=1.0

# Optional: verify Google Chat request identity (recommended in production)
# google-auth>=2.30

# Optional: export the models to ONNX for the browser frontend
# (python -m sodachat.export_onnx). Export-time only — sodachat.web serves
# static files and the models then run in the page, not here.
# onnx>=1.17
# onnxruntime>=1.20
# onnxscript>=0.1
